let modal;document.addEventListener('DOMContentLoaded',async()=>{const category=document.getElementById('kategori');if(category)category.innerHTML='<option value="">Semua kategori</option><option>Surat Masuk</option><option>Surat Keluar</option><option>Laporan</option><option>Dokumentasi</option>';const body=document.getElementById('tb'),frame=document.getElementById('pdfFrame');modal=new bootstrap.Modal(document.getElementById('previewModal'));try{const data=await api.list();body.innerHTML='';data.forEach(r=>{const row=document.createElement('tr');[r.nomor,r.judul].forEach(value=>{const cell=document.createElement('td');cell.textContent=value||'';row.appendChild(cell);});const preview=document.createElement('button');preview.className='btn btn-sm btn-primary';preview.textContent='Preview';preview.onclick=()=>lihat(r.file,frame);const previewCell=document.createElement('td');previewCell.appendChild(preview);row.appendChild(previewCell);const qrCell=document.createElement('td'),canvas=document.createElement('canvas');canvas.width=42;canvas.height=42;qrCell.appendChild(canvas);row.appendChild(qrCell);const actionCell=document.createElement('td');const edit=document.createElement('button');edit.className='btn btn-sm btn-warning';edit.textContent='Edit';edit.onclick=()=>editMeta(r.id,r.judul,edit);const remove=document.createElement('button');remove.className='btn btn-sm btn-danger ms-1';remove.textContent='Hapus';remove.onclick=()=>hapus(r.id,remove);actionCell.append(edit,remove);row.appendChild(actionCell);body.appendChild(row);if(window.QRious&&r.file)new QRious({element:canvas,value:r.file,size:42});});}catch(err){body.innerHTML='<tr><td colspan="5">Gagal memuat arsip</td></tr>';console.error(err);}});function lihat(url,frame){frame.src=url;modal.show()}async function editMeta(id,judul,button){const n=prompt('Judul baru',judul);if(n&&n.trim()){try{if(button)button.disabled=true;setLoading(true,'Menyimpan perubahan...');await api.update({id,judul:n.trim()});setLoading(false);await notify('Berhasil','Judul arsip berhasil diubah','success');location.reload();}catch(err){setLoading(false);if(button)button.disabled=false;notify('Gagal mengubah arsip',err.message,'error');}}}async function hapus(id,button){if(confirm('Pindahkan ke arsip?')){try{if(button)button.disabled=true;setLoading(true,'Menghapus arsip...');await api.trash(id);setLoading(false);await notify('Berhasil','Arsip dipindahkan ke tempat sampah','success');location.reload();}catch(err){setLoading(false);if(button)button.disabled=false;notify('Gagal menghapus arsip',err.message,'error');}}}