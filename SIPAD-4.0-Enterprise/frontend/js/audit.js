async function loadAudit(){const body=document.getElementById('tbody');try{const logs=await api.audit();body.innerHTML='';logs.forEach(l=>{const row=document.createElement('tr');[l.waktu,l.user,l.aksi,l.nomor].forEach(value=>{const cell=document.createElement('td');cell.textContent=value||'';row.appendChild(cell);});body.appendChild(row);});}catch(err){body.innerHTML='<tr><td colspan="4">Gagal memuat audit</td></tr>';console.error(err);}}loadAudit();
