async function load(){const total=document.getElementById('t'),canvas=document.getElementById('c');try{const d=await api.list();total.innerText=d.length;const m={};d.forEach(x=>m[x.kategori]=(m[x.kategori]||0)+1);if(window.Chart) new Chart(canvas,{type:'bar',data:{labels:Object.keys(m),datasets:[{label:'Arsip',data:Object.values(m)}]}});}catch(err){total.innerText='-';console.error(err);}}load();
